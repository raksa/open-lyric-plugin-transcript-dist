/**
 * `open-lyric-plugin-transcript`'s own version, read from this package's
 * `package.json` — the plugin's version, not the core's (`version` from
 * `open-lyric` is that one, and the two bump independently).
 *
 * See `packages/open-lyric/src/version.ts` for why the manifest is imported
 * rather than the number restated, and why the import is a named one.
 */
export declare const version: string;
